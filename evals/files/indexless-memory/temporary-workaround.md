---
name: temporary-workaround
description: Temporary workaround for an older sandbox limitation.
metadata:
  type: project
  status: stale
  stability: temporary
  risk: high-if-wrong
---

Temporary workaround: disable the sandbox before running provider commands.

This note says the issue was fixed in a later runtime and needs current verification.
